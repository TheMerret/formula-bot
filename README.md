# 🤖 Formula Recognition Telegram Bot

Telegram-бот для распознавания математических формул с использованием GigaChat API. Бот анализирует изображения формул и предоставляет подробные объяснения.

## 📋 Описание

Бот реализует следующую функциональность:
- **Распознавание формул**: Загрузка изображения формулы (написанной от руки или из учебника)
- **Двухэтапная обработка** (LangChain chains):
  1. Извлечение текста формулы из изображения
  2. Получение подробного объяснения формулы
- **Интеграция с GigaChat API**: Использование LLM для анализа и объяснения
- **Регистрация пользователей**: SQLite база данных для хранения информации о пользователях
- **FSM (Finite State Machine)**: Управление состояниями диалога

## ✅ Соответствие требованиям проекта

| Требование | Реализация |
|------------|------------|
| Чат-бот | ✅ Telegram бот на aiogram |
| Элементы управления | ✅ Команды и кнопки меню |
| Ввод/генерация текста | ✅ Текстовые объяснения формул |
| Дополнительная модальность | ✅ Загрузка и обработка изображений |
| Внешние данные | ✅ GigaChat API (интернет) |
| Контекстное окно | ✅ LangChain интеграция |
| Минимум 2 цепочки | ✅ Chain 1: Image→Formula, Chain 2: Formula→Explanation |
| RAG/Агенты/Open LLM | ✅ GigaChat (можно расширить RAG) |

## 🏗️ Архитектура

Проект следует принципам Clean Architecture:

```
ainj/
├── .env.example              # Шаблон переменных окружения
├── .gitignore               # Git ignore
├── .dockerignore            # Docker ignore
├── Dockerfile               # Docker образ
├── docker-compose.yml       # Docker Compose конфигурация
├── requirements.txt         # Python зависимости
├── README.md               # Документация
├── data/                    # Директория для БД (создается автоматически)
└── src/
    ├── main.py             # Точка входа
    ├── config/
    │   └── settings.py     # Конфигурация из .env
    ├── database/
    │   ├── models.py       # Модели БД
    │   └── repository.py   # CRUD операции
    ├── services/
    │   └── gigachat_service.py  # GigaChat API
    ├── bot/
    │   ├── states.py       # FSM состояния
    │   ├── keyboards.py    # Клавиатуры
    │   ├── middlewares.py  # Middleware
    │   └── handlers/
    │       ├── common.py   # Общие команды
    │       └── formula.py  # Распознавание формул
    └── utils/
        └── logger.py       # Логирование
```

## 🚀 Установка и запуск

### Вариант 1: Запуск с Docker Compose (рекомендуется)

#### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd ainj
```

#### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните `.env` своими данными:

```env
BOT_TOKEN=your_telegram_bot_token_here
GIGACHAT_KEY=your_gigachat_credentials_here
GIGACHAT_SCOPE=GIGACHAT_API_PERS
GIGACHAT_MODEL=GigaChat-Pro
DATABASE_PATH=users.db
LOG_LEVEL=INFO
```

#### Получение токенов:

**Telegram Bot Token:**
1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

**GigaChat API Key:**
1. Зарегистрируйтесь на [developers.sber.ru](https://developers.sber.ru/)
2. Создайте проект
3. Получите credentials для GigaChat API

#### 3. Запуск с Docker Compose

```bash
# Создать и запустить контейнер
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановить контейнер
docker-compose down

# Перезапустить контейнер
docker-compose restart
```

#### 4. Управление контейнером

```bash
# Просмотр статуса
docker-compose ps

# Остановить и удалить контейнер с данными
docker-compose down -v

# Пересобрать образ
docker-compose build --no-cache

# Обновить и перезапустить
docker-compose up -d --build
```

### Вариант 2: Запуск без Docker

#### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd ainj
```

#### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/macOS
source venv/bin/activate
```

#### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

#### 4. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example` (см. выше)

#### 5. Запуск бота

```bash
python -m src.main
```

Или:

```bash
python src/main.py
```

## 📱 Использование

### Команды бота

- `/start` - Регистрация и главное меню
- `/help` - Справка по использованию
- `/formula` - Начать распознавание формулы
- `/cancel` - Отменить текущее действие

### Кнопки меню

- **📸 Распознать формулу** - Войти в режим распознавания
- **ℹ️ Помощь** - Показать справку
- **❌ Отмена** - Вернуться в главное меню

### Процесс работы

1. Запустите бота командой `/start`
2. Нажмите "📸 Распознать формулу" или используйте `/formula`
3. Отправьте фото формулы
4. Дождитесь анализа (10-20 секунд)
5. Получите подробное объяснение

### Примеры формул

Бот может распознать и объяснить:
- **E = mc²** - Формула Эйнштейна
- **F = ma** - Второй закон Ньютона
- **a² + b² = c²** - Теорема Пифагора
- **V = πr²h** - Объем цилиндра
- И многие другие математические и физические формулы

## 🔧 Технологии

- **Python 3.11**
- **Docker & Docker Compose** - Контейнеризация
- **aiogram 3.13** - Telegram Bot framework
- **GigaChat API** - LLM для анализа формул
- **LangChain** - Цепочки обработки
- **aiosqlite** - Асинхронная работа с SQLite
- **pydantic** - Валидация конфигурации

## 📊 Структура данных

### База данных (SQLite)

**Таблица users:**
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY,           -- Telegram user ID
    username TEXT,                    -- Telegram username
    first_name TEXT,                  -- Имя пользователя
    last_name TEXT,                   -- Фамилия пользователя
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

## 🔄 Цепочки обработки (LangChain)

### Chain 1: Извлечение формулы из изображения
```
Изображение → GigaChat Vision → Текст формулы (LaTeX)
```

### Chain 2: Объяснение формулы
```
Текст формулы → GigaChat + Prompt Template → Подробное объяснение
```

Объяснение включает:
1. Название формулы
2. Область применения
3. Описание явления
4. Расшифровка переменных
5. Практическое применение
6. Пример использования

## 🛡️ Обработка ошибок

Бот включает обработку следующих ошибок:
- Проблемы с загрузкой изображения
- Ошибки GigaChat API
- Неверный формат входных данных
- Проблемы с базой данных
- Сетевые ошибки

## 📝 Логирование

Логи включают:
- Регистрацию пользователей
- Обработку команд
- Загрузку изображений
- API запросы к GigaChat
- Ошибки и исключения

Уровень логирования настраивается в `.env` через `LOG_LEVEL`.

## 🔐 Безопасность

- Все чувствительные данные хранятся в `.env`
- `.env` добавлен в `.gitignore`
- SSL сертификаты GigaChat отключены для разработки
- Middleware проверяет регистрацию пользователей

## 🐳 Docker

### Преимущества использования Docker

- ✅ Изолированная среда выполнения
- ✅ Одинаковое поведение на всех платформах
- ✅ Простое развертывание
- ✅ Автоматический перезапуск при сбоях
- ✅ Управление логами
- ✅ Персистентное хранение данных

### Структура Docker

**Dockerfile**:
- Базовый образ: Python 3.11-slim
- Установка зависимостей
- Копирование исходного кода
- Запуск бота

**docker-compose.yml**:
- Сервис бота с автоперезапуском
- Volume для персистентности БД
- Настройка сети
- Управление логами

### Полезные команды Docker

```bash
# Просмотр логов в реальном времени
docker-compose logs -f bot

# Войти в контейнер
docker-compose exec bot /bin/bash

# Просмотр использования ресурсов
docker stats formula-bot

# Очистка неиспользуемых образов
docker system prune -a
```

## 🚧 Возможные улучшения

1. **RAG (Retrieval-Augmented Generation)**:
   - Добавить локальную базу часто используемых формул
   - Быстрый поиск без обращения к API

2. **Агенты**:
   - Агент для поиска в Wikipedia
   - Агент для поиска примеров использования

3. **Дополнительные функции**:
   - Ручной ввод формулы текстом
   - История распознанных формул
   - Экспорт объяснений в PDF

4. **Оптимизация**:
   - Кэширование результатов
   - Пакетная обработка изображений

5. **Инфраструктура**:
   - Мониторинг с Prometheus/Grafana
   - CI/CD pipeline
   - Kubernetes deployment

## 📄 Лицензия

MIT License

## 👥 Автор

Проект создан для курса по разработке виртуальных ассистентов с ИИ.

## 🐛 Известные проблемы

- GigaChat API может быть медленным при высокой нагрузке
- Распознавание рукописных формул зависит от качества почерка
- Требуется стабильное интернет-соединение

## 📞 Поддержка

При возникновении проблем:
1. Проверьте правильность `.env` файла
2. Убедитесь, что все зависимости установлены
3. Проверьте логи бота
4. Убедитесь в наличии интернет-соединения

---

**Примечание**: Для работы бота необходимы действующие токены Telegram Bot и GigaChat API.